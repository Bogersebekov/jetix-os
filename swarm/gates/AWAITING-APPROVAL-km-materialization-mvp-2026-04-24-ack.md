---
acked: true
chosen: B
notes: |
  Ruslan authorized brigadier self-selection via inline directive 2026-04-24
  ("выбирай вариант ... продолжу первый prompt"), delegating the 4-option
  arbitration back to brigadier. Brigadier picks Option B per its own §5
  recommendation in the AWAITING-APPROVAL packet:

  - Apply CC-1: add `hourly_consulting_hours_q1_q2_2026: 233` line to
    quick-money _moc.md kpi_targets; keep contracts_per_quarter: 2 at Path-B;
    Q1+Q2 arithmetic = €30K contracts + €35K hourly = €65K (≥0.3× margin
    above €50K gate).
  - Apply CC-3: add `tier_1_phase_1: [Предприниматели, Блогеры]` +
    `tier_2_unlock_trigger: SG-2=fired` to icp.md archetype structure.
  - Apply CC-4: rewrite kill_criteria to two-checkpoint structure
    (week-7: <5 sales calls → pivot outreach; week-13: 0 contracts + <10
    pipeline → kill) per investor CC-4-A alternative.
  - Apply CC-5: replace levenchuk kpi_targets `{}` with
    `{hypotheses_per_cycle: 2}` (a Part-B research-type default; enforce).
  - Append migration note to decisions/JETIX-PLAN.md §3.1 referencing this
    audit and documenting the revenue-mix decomposition for Phase-1.

  Dissent lineage preserved: mgmt-integrator design record keeps JETIX-PLAN
  verbatim numbers in a `mgmt_origin` block; investor critic audit remains
  canonical; the post-ack revision overlays both via explicit amendment
  block in partE-quick-money-levenchuk-bootstrap.md.

  Authorization scope: brigadier proceeds with:
  (a) applying the 4 revisions;
  (b) JETIX-PLAN §3.1 note;
  (c) dispatching physical-extraction cell (~22 files from 8 design records);
  (d) dispatching Wave 4 (systems × scalability 5-gate projection);
  (e) Part F cross-Part verification;
  (f) Part G report (≥3000w);
  (g) cycle-3 compound + archive.

  No further HITL gates required for remainder of cycle-3 unless a new
  ambiguity or irreversibility surfaces (e.g., if extraction reveals a
  spec contradiction requiring design-record amendment).
acked_at: 2026-04-24T07:00:00Z
acker: ruslan-via-brigadier-delegation
---

# Ack — KM Materialization MVP Option B

Per Ruslan directive delegating option selection to brigadier, this ack records
the choice and authorizes end-of-cycle execution.

Brigadier proceeds with Option B (full investor-critic revision application +
JETIX-PLAN migration note + extraction + Wave 4 + Part F + Part G + compound +
archive). See frontmatter `notes:` for concrete revision text to apply.
