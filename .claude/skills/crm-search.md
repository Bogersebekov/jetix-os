---
name: crm-search
description: Full-text search over CRM records (name, aliases, body §1+§13, tags). Ranked by hits.
type: skill
---

# /crm-search <query>

```bash
cd ~/jetix-os
python3 -m crm._scripts.crm search "$@"
```

Example: `/crm-search "DACH AI"`
